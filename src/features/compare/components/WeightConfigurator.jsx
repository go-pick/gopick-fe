import React from 'react';
import S from '../Compare.styles';
import styled from 'styled-components';

const WeightConfigurator = () => {
	return (
		<div style={{minHeight: "70vh"}}>
			<S.SubTitle style={{fontWeight: 800}}>당신이 중요하게 생각하는 것은?</S.SubTitle>
		</div>
	);
};

export default WeightConfigurator;

